# 汇流 Congrove v0.5 需求文档

| 项 | 值 |
|---|---|
| 版本 | **v0.2（2026-08-08 回填）**——v0.1 是 08-07 与 liaoruili 逐项对齐的初稿 |
| 分轨 | **全流程轨**（重大重构 + 新数据模型 → 相位 0→6 全走） |
| 相位 | **0（需求 PRD）已签核，本次为回填修订** |
| v0.2 改了什么 | ① 新增 5 条决策：**F4** 跨度上限 30 天 / **J1b-2** 材料区回收站 / **J1c** 超管读不到 + 影子账户 / **L0b** 补录对方完全看不见 / **H2** 自动转写覆盖所有类型；② 订正本文自身三处**事实错误**（§I 的「status 仍是四态」漏了 `tentative`、`reminded_at` 语义错、`activities` 缺 `deleted_at`）；③ **L3** 配额口径改「算到项目 owner」 |
| 前置 | `PRD-meetings.md`（v0.3，会议模块）与 `STORY-MAP.md` 仍然有效的部分在本文中显式引用；被推翻的会标出来 |

> ★这一版是重构，不是增量★：线上部署已停、数据库已删，
> 所以**不走迁移路线**——`migrations/` 压成一个干净的 `0001_init.sql` 按新模型直接建表。
> 这是唯一能这么做的时刻（`_sqlx_migrations` 里没有任何实例的记录），上一次是 2026-08-05。

---

## 0. 背景与目标

### 0.1 这一版要解决什么

v0.4 做完了「会议协同」：约会、应答、忙闲、材料、纪要。
但它把自己框死在**会议**这一件事上，而用户真正想要的是**记录自己的时间都花在哪了**——
读文献、跑数据、写作、开会、看病、健身。会议只是其中一类。

**v0.5 的一句话目标**：让「汇流」从一个开会工具，变成**一个人一整天的时间记录与协作平台**。

### 0.2 目标

1. **活动模型泛化**：会议降级为「活动的一种类型」，类型可自建（A 节）；
2. **能记录，不只是能安排**：支持补录过去、支持提醒（F 节）；
3. **时间看得清**：日历压缩空白时段、身份一眼可辨、跨时区不出错（C/D/E/G 节）；
4. **归档不等于消失**：结题项目的历史照样在日程和统计里（B 节）。

### 0.3 ★非目标★（这一版明确不做）

写清非目标是为了**防止范围蔓延**——下面每一条都是有人可能提、而我们这一版决定不做的：

| 不做 | 为什么 |
|---|---|
| **浏览器桌面通知 / 邮件提醒** | 真后台推送要 Service Worker + VAPID；邮件要平台通道。这一版靠站内信 + 页面弹窗（F2），**已知代价：人不在电脑前会错过** |
| **自建类型带参与人/纪要能力** | 会把「输个名字就能用」变成「填一张表」（A3） |
| **跨人共享自建类型 / 团队类型字典** | 每人一套（A2），团队统一口径这一版不做 |
| **移动端 App / 小程序** | Web 响应式即可 |
| **日历双向同步（订阅进来、写回 Google Calendar 等）** | 只出不进的 `.ics` 导出可以考虑，双向同步是一致性泥潭 |
| **AI 自动识别活动类型 / 自动填时长** | 记录的准确性比省那两下点击重要 |
| **多人共享的「团队日历」视图** | 忙闲查询已覆盖「他什么时候有空」这个真需求 |

### 0.4 成功判据

一个人用两周之后，**他自己的日程页是满的**——不是因为他被拉进了很多会，
而是因为他把自己做的事记了进去；并且他能说出「我这两周有多少时间花在 X 上」。

---

## 1. 角色

沿用 v0.4，无变化：

| 角色 | 是什么 |
|---|---|
| **超管** | 平台白名单（`CONGROVE_SUPER_USERS`），跨项目可见，用于救火与治理 |
| **项目主持人（owner）** | 每个项目唯一；可归档/删除项目、转移主持人（需对方接受） |
| **项目管理员（admin）** | 可加人、改设置 |
| **项目成员（editor）** | 可写内容、发起活动 |
| **只读成员（viewer）** | 只能看 |
| **活动发起人 / 记录员 / 参与人 / 旁听者** | 活动内的身份，★不携带任何资料权限★（资料权限只来自项目成员身份） |

---

## 2. 概念模型

```
用户 ──┬─ 属于多个 项目（角色:viewer/editor/admin，owner 唯一）
       │
       ├─ 拥有自己的 活动类型（自建，每人一套 + 系统预置两种）
       │
       └─ 参与/发起 活动 ──┬─ 有一个 类型（决定它有哪些能力）
                            ├─ 关联 0..N 个 项目（会议必须 ≥1；个人记录可以 0）
                            ├─ 有 0..N 个 参与人（所有类型都可以；★过去的活动不发通知★）
                            ├─ 有 0..1 份 纪要（仅当类型 has_minutes）
                            └─ 有 0..N 份 材料/录制（所有类型都可以）
```

**两条贯穿全局的规则**（从 v0.4 继承，仍然成立）：

1. ★**资料权限的唯一来源是「当前项目成员身份」**★（原 D3）——
   参与一场活动**不给**任何资料权限；离开项目即失去全部，含他参与过的活动。
   因此绝不能加「授权生效时间」这类字段。
2. ★**一个活动可以关联多个项目，材料整份进每个关联项目**★（原 D4）。

---

## A. 活动模型（原「会议」）★2026-08-07 已确认★

### A0. 「会议」重构为「活动」，会议成为活动的一种类型

**为什么**：下一版要让用户**记录每天的日程**——读文献、跑数据、写作、看病、健身。
叫「会议」就把这件事框死了：大部分日程根本不是会。
**活动**是那个更大的容器，**会议只是其中一种类型**。

这不是改名。真正的问题是：现在的硬约束**大多只对会议成立**——

| 现有硬约束 | 对「会议」 | 对「下午去医院」 |
|---|---|---|
| 记录员必填（D14） | 合理 | 荒谬 |
| 必须关联至少一个项目（D3 材料权限来源） | 合理 | 别扭：难道为「跑步」建个项目 |
| 参与人 / 应答 / 忙闲 / 纪要 / 录制转写 | 合理 | 全都用不上 |

### A1. ★类型决定能力，不是纯标签★

类型带一组**能力位**，一个活动要校验什么、详情页出不出纪要区，**全部从类型推导**。

★2026-08-07 精简：能力位只剩三个★

| 能力位 | 会议 | 个人日程 | 用户自建 |
|---|---|---|---|
| `has_minutes` 纪要与记录员 | ✅ | ❌ | ❌ |
| `needs_project` 必须关联项目 | ✅ | ❌ | ❌ |
| `busy` 产生忙闲 | ✅ | 用户可改 | **用户可改** |

**参与人 / 材料 / 补录对所有类型统一开放**——
★一个字段用不用是用户的事，不是类型替他决定的★：
读书会也可能是几个人一起读，「跑数据」也可能想挂一份脚本。

被砍掉的三个能力位各有理由：

| 砍掉 | 为什么 |
|---|---|
| ~~`has_participants`~~ | 参与人**统一**（L0）：判据不是类型，而是**时间过没过**——过去的不发通知 |
| ~~`has_materials`~~ | J0 已定个人活动也能传材料（落「我的活动材料」），这个位自相矛盾 |
| ~~`allow_past`~~ | 同上，所有类型都能填任意时间，只是过去的不发通知 |

**为什么留下的三个不能做成纯标签**：它们各自都有**硬后果**——
`has_minutes` 决定「记录员是不是必填」、`needs_project` 决定「材料有没有权限归属（D3）」、
`busy` 决定「别人看不看得到你忙」。★纯标签意味着这三条校验全都做不了★，
而 UI 只能写 `if (type === '会议')`，新增一个类型就要改代码。

### A2. 自建类型：★每人一套★

我建的类型只有我自己看得到。

- **个人日程本来就是私事**——你的「摸鱼」不该出现在别人的下拉里，也不该被别人改名/删掉；
- 代价是团队想统一口径（都叫「读文献」）做不到。**接受这个代价**：
  跨人统计本来就该按「活动类型名」这种软口径聚合，而不是靠强制共享一套字典。

### A3. 自建类型★只开一个开关：占不占忙闲★

自建 = 输一个名字就能用，**不需要理解任何概念**。
唯一的开关是「占不占忙闲」——因为**那是唯一真正影响别人的属性**：
它决定别人约你时看不看得到你这段时间是忙的。

参与人 / 纪要 / 材料这些能力**不开放给自建**：带这些能力的类型由系统预置。
→ 推论：**「预置类型列表」就是「带参与人的类型」的完整集合**。

### A4. 不关联项目的活动：默认仅自己可见，可改公开

- 默认**仅自己**：内容（标题、备注）只有自己看得到；
  若类型勾了「占忙闲」，别人查我忙闲时只看到**一个无标题的时间块**，看不到是什么事。
  ⚠ ★原文这里写着「这与现在『私密项目』的规则一致」，**那句话不准确，2026-08-08 订正**★
  （决策不变，只是把论据改对）：v0.4 里「只关联私密项目的会」是**完全不产生忙块**的，
  不是「无标题时间块」。所以这不是「一致」，而是★把判据从**项目级**换到**活动级**★——
  想让某一条不占忙闲，v0.4 要去改**整个项目**的可见性（很钝，连累项目里所有会），
  v0.5 只要动这条活动的 `busy` 开关。**是更细、更受用户控制，不是隐私回退。**
  ⚠ 另注意：`visibility`（别人看不看得到**内容**）与 `busy`（别人看不看得到我**忙**）
  是**两个正交的开关**，都由用户控制。别把它们混成一个——混了就会得出「公开性变了 = 忙闲变了」这种错结论。
- 可改**公开**：给「我在做什么想让人知道」的场景留出口（公开后全平台可见）。

### A5. 系统只预置两种类型

| 预置类型 | 能力 |
|---|---|
| **会议** | 全能力（参与人 / 纪要 / 材料 / 必须关联项目 / 占忙闲） |
| **个人日程** | 简单型（只有标题、时间、备注）。★`busy_default = **false**`★（2026-08-08 liaoruili 拍板）——判据是「占忙闲 = **影响别人**」，而会议本来就是多人的事、个人日程本来不是。想让某一条占忙闲，建/改活动时自己勾。 |

**预置多了是替用户做决定**——不同课题组的分类习惯差别很大，
而下拉里用不上的项只会碍事。自建成本已经低到「输个名字」，不必替他们预设。

### A6. 命名映射（改名范围）

| 旧 | 新 |
|---|---|
| `meetings` | `activities` |
| `meeting_participants` / `meeting_projects` / `meeting_messages` / `meeting_minutes` / `meeting_link_history` / `meeting_reads` | `activity_*` |
| `items.meeting_id` | `items.activity_id` |
| `/api/meetings*` | `/api/activities*` |
| 界面「发起会议」 | 「新建活动」 |
| 界面「参会人」 | 「参与人」 |
| 「会议纪要」「记录员」「旁听」 | 保留原词，但**只在带对应能力的类型上出现** |

新增表：`activity_types`（含预置两行 + 每人自建的行）。

---

## B. 项目归档（推翻 D17 的一部分）★2026-08-07 已确认★

### B0. 归档项目的历史活动★照常出现在日程里★

**推翻 D17 的日历过滤**。当初我写的理由是「日历回答『我接下来要做什么』，
塞满已结题项目的历史会议会变成考古现场」——这个视角是片面的。

liaoruili 的视角：**日程也是「我做过什么」的记录**。
「有时候用户就想回顾之前的工作，看看满日程的很有成就感」——
归档不该让过去消失，那些时间是真的花掉了。

### B1. 显示但★淡化 + 「已归档」标记★

照常出现在日历里，但颜色更淡、带一个小标。

**为什么不做成完全一样**：归档项目是**只读**的（D17 的这一半仍然成立）——
不加区分的话，用户会点进去想传材料、想改时间，才发现动不了，
而界面事先没给任何提示。★标记是为了让「动不了」这件事在点进去之前就可见★。

### B2. ★有未开始的活动就不允许归档★

归档 = 做完了。如果还有排在未来的活动，那就是没做完 —— 直接拒绝归档，
要求先处理（取消它们，或等它们开完）。

★「未开始的活动」**不含已取消的**★（2026-08-07 确认）——
取消了就不算有安排，不该因为一条作废的记录卡住归档。

推论：**归档项目里不会存在「未来的活动」**，
所以「归档项目的活动要不要占忙闲」这个问题自然消失（历史活动不产生忙闲）。

⚠ **实现要注意的边界：跨项目的活动**。
A 项目想归档，却卡在一场**同时关联 A 和 B** 的会上，而那场会主要是 B 的事。
处置：★错误信息里列出是哪几场★，让用户自己决定——
他可以把 A 从那场会的关联项目里去掉，也可以直接取消它。
**不要静默跳过跨项目的活动**：那等于允许「项目归档了但它名下还有未来的会」。

### B3. 归档项目的活动★计入统计★

那是真实发生过的投入，不该因为项目结题就从「我今年花了多少时间」里消失。
★否则会出现一个荒谬的现象：项目一归档，你今年的工作量就凭空少了一大块★。

分项目列表里带「已归档」标记区分。

---

## C. 日程上的身份标识与 hover ★2026-08-07 已确认★

### C0. 日历块要标出「我在这场活动里是什么身份」

现在日历块只按**可见性 + 应答状态**着色（公开=青实框 / 私密=紫虚框 / 待应答=红），
看不出「这场是我张罗的」还是「我只是去听听」。

### C1. 标三种身份：★发起人 / 记录员 / 旁听★

| 身份 | 标 | 为什么 |
|---|---|---|
| 我发起的 | ✅ | 我要对它负责 |
| 我是记录员 | ✅ | ★唯一带「欠着一份活」的身份★——会后要整理纪要，在日历上看得到比事后被催强 |
| 我只是旁听 | ✅ | 与「参与」性质不同：不用答复、拿不到材料，标出来免得误以为自己是正式参会人 |
| 普通参与人 | ❌ **不标** | 默认状态不该占视觉。全标的话满屏都是同一个图标 = 等于没有图标，反而把真正特殊的那几个淹了 |

### C2. 形式：★标题前的小图标★（AntD 图标，不用 emoji）

发起=星标 / 记录员=笔 / 旁听=空心圈。

⚠ ★不用 emoji★：容器里没有 emoji 字体时会显示成豆腐块——
截图脚本已经踩过这个坑（`e2e/shot.mjs` 里有记录）。

不选「左侧竖色条」是因为**颜色不能自解释**，得靠图例记；
也不做「图标+色条双重编码」——一个 30px 的小块上堆颜色、边框、图标、标题四种信息，容易花。

### C3. hover 显示：完整标题 + 时间 + 所属项目 + 我的身份

★标题必须在里面★：块小的时候标题常被截成「模型评…」，
**hover 是看全它的唯一机会**——如果 tooltip 里只放项目，那被截断的标题就永远看不全了。

不放参与人数/地点/链接：tooltip 一大就会遮住相邻的时间格，
而那些信息点进去就有。

---

## D. 日历时段收缩 ★2026-08-07 已确认★

### D0. 凌晨 0–8 点压缩成 ★2 小时一格★

那段时间基本没人安排，却占掉日历近三分之一的高度。

### D1. 压缩区里有活动时★照样压缩，块变矮★

不做「有活动就自动展开」——整周行高统一，布局简单可预期。

⚠ **代价与缓解**：一个 1 小时的活动在 2h/格 的区域里只有 ~15px 高，点不中也读不出。
所以实现上给压缩区的块一个**最小高度保底（≈18px）**，
并依赖 C3 的 hover（完整标题 + 时间 + 项目 + 身份）把看不清的信息补回来。
★这里「块高 = 时长」的视觉契约本来就已经被压缩打破了★，保底不再多破坏什么。

### D2. 晚上★不收缩★，保持压暗

★凌晨和晚上不是一回事★：前者几乎没人安排，后者只是不在工作时间——
晚餐会、跨时区的会、自己晚上写东西都真实存在。

---

## E. 时区 ★2026-08-07 已确认★

### E0. 用户在设置里设当地时区，★不设默认北京★

不做浏览器自动探测。

⚠ **已知代价**：没设过的海外用户会看到全错的时间，而他未必意识到要去设置里改。
**缓解（实现时做）**：检测到浏览器时区与设置不一致时，在日程页顶上给一条**可关闭的提示条**——
「你的设备时区是 America/New_York，当前按 Asia/Shanghai 显示，去切换？」
★这不违背「手动设」这个决定★，只是让用户知道有这回事，而不是默默看错时间。

### E1. 建活动时可选**这场活动的时区**，显示时自动换算成看的人的时区

- 存储一律 UTC（`timestamptz`），时区名（IANA，如 `Asia/Shanghai`）单独存一列；
- 活动的时区是**输入时的解释**：「我在纽约给北京的组会排 15:00」——指的是北京时间 15:00；
- ★也是周期规则展开的基准★：「每周三 15:00 北京时间」跨夏令时不会整体漂一小时
  （这正是数据模型里坚持存 IANA 名而不是固定偏移的原因）。

### E2. ★跨时区时才标注原始时区★

活动时区与我的设置一致 → 不标（国内用户 99% 的情况看不到任何多余的字）；
不一致 → 时间后面补一句「（北京 15:00）」。

**为什么必须标**：跨时区的人看到「凌晨 3:00」会懵——
不知道这是对方的下午，还是真要自己凌晨爬起来。★那个数字必须有个解释★。

---

## F. 补录与提醒 ★2026-08-07 已确认★

### F0. ★所有类型都能填任意时间★（`allow_past` 能力位已废弃）

原本打算按类型区分「能不能排过去的时间」，后来发现**不需要**（见 L0）：

> **所有活动都能填任意时间；过去的不发通知。**

- 补录一场临时会（用「会议」类型、时间填昨天）→ 允许，不发通知；
- 「昨天下午改论文改了 3 小时」→ 允许，不发通知。

★原来那条「会议不能排过去的时间」的校验去掉★——
它当初是为了防「约一个已经过去的会」的误操作，
但**补录一场已经发生的会是正当的**，而误操作由 F1 的「超 7 天确认」兜住。

### F1. 补录不限时间，但★过久的给个确认★

想补多久以前的都行——**这是自己的记录，不是报销**，
回头整理上个月做了什么是正当需求，加硬限制只会挡住真实用法。

★超过 **7 天** 就确认★（2026-08-07 liaoruili 定，我原本提的 90 天太松）：
弹一句「这是 7 月 28 日的，确认吗？」。

**为什么 7 天而不是 90 天**：真实的补录多半是「昨天/前天忘了记」——
一周之内。跨过一周还在补，就已经值得停下来看一眼日期对不对了。
90 天那个阈值太松，等于这道闸只挡得住「输错年份」这一种错，
而**输错月份、输错日期**才是更常见的手滑。

### F2. 提醒：★站内信 + 页面内弹窗★

- **站内信**是现成的（平台 registry，汇流已在用），不需要新基建；
- 页面开着时额外弹一个提醒框。

★不做浏览器桌面通知 / 邮件★：桌面通知关了页面就收不到（真后台推送要
Service Worker + VAPID，工作量大得多）；邮件要先确认平台有没有可用通道。
**已知代价：人不在电脑前就真错过了。** 这一版接受。

### F3. 提醒时长：★个人全局默认 + 单个活动可改★

设置里定一个默认（如提前 15 分钟），建/改活动时可以单独改这一场。
**和手机日历一样，人不用学**；又能给「要开车去的那场会」单独设提前 1 小时。

可选值：不提醒 / 5 分 / 15 分 / 30 分 / 1 小时 / 1 天 / 自定义。

⚠ **补录的活动不发提醒**（时间已经过去了，提醒它没有意义）——
实现时要显式跳过，否则批量补录会一次炸出一堆无意义的站内信。

### F4. 一条活动的跨度上限 **30 天** ★2026-08-08 liaoruili 拍板★

**为什么需要一个上界**：现在只有「结束必须晚于开始」这一条下界。
一条 `2026-08-08 → 2126-08-08` 的活动（输错年份）会命中**每一次**日历查询与忙闲查询的时间窗，
把两条最热的路径一起拖垮，而用户在任何一屏上都看不出是哪条记录干的。

**为什么是 30 天而不是更松**：真实的长活动是「出差两周」「年假一个月」。
更常见的手滑不是输错年份，而是**输错月份**（8 月 8 日打成次年 3 月 1 日 = 205 天）——
365 天的上界挡不住它，30 天挡得住。

- 上界是 **30 天**，超过就拒绝；
- ⚠ **必须在应用层先校验并返 400 带可读文案**。只靠数据库 CHECK 的话，
  本仓 `error.rs` 把 sqlx 错误一律映射成 500 `"internal error"` ——
  用户会拿到一个「服务器出错了」，而问题其实出在他填的日期上。
- ⚠ 连带：`actual_minutes`（会后补录的实际时长）的上界原来写死 1440 分钟（一天），
  现在改成**按这场活动的跨度算**，否则三天的出差填不了实际时长。

---

## G. 时间输入的手感（参考腾讯会议）★2026-08-07 已确认★

### G0. 开始时间默认当前时间，★取不取整按类型分★

| 类型 | 默认值 | 为什么 |
|---|---|---|
| 会议 | 向上取整到下一个 **15 分钟**（15:37 → 15:45） | ★没人约 15:37 开始的会★；参会人看到这种时间会以为发起人填错了 |
| 个人记录 / 自建 | **就用 15:37** | 那是「我刚做完，现在记」的真实时刻，取整反而要人手动改回去 |

与 A1（类型决定行为）一致：**同一个控件，行为由类型推导，不写死**。

### G1. 结束时间给★持续时长快捷选项★

不再只有一个绝对时间选择器。可选：**15 分 / 30 分 / 1 小时 / 1.5 小时 / 2 小时 / 3 小时 / 自定义**，
默认 **1 小时**。

★两者双向绑定★：
- 选「1 小时」→ 结束时间自动算出来；
- 直接改结束时间 → 时长下拉跟着显示成对应值（不在预设里就显示「自定义」）。

**为什么值得做**：绝大多数活动的时长是那几个常用值，
而「选一个绝对的结束时刻」要人在脑子里做一次加法——
★加法本身不难，难的是它出现在每一次创建里★。

⚠ 跨天的活动（如「出差 3 天」）走「自定义」，快捷选项只覆盖小时级。

---

## J. 「我的活动材料」与命名 ★2026-08-07 已确认★

### J0. 不关联项目的个人活动★也能传材料★

推翻我原来提的「不能传」。材料要落到某个项目才有权限归属（D3），
所以给每人一个系统默认的存档区：**「我的活动材料」**。

### J0b. 结构：★按「日期 + 活动名」分组★，要改就回活动去改

```
我的活动材料/
  2026-08-08 读文献 Acemoglu 2024/
    acemoglu-2024.pdf
    我的批注.md
  2026-08-07 田野访谈/
    录音.m4a
  2026-08-05 健身/
```

**任何更改都回到那条活动去做**——改名、删除、加材料，入口都在活动详情页。
这个区只是「把散落在各条个人活动里的东西汇到一处看」。

#### ★实现要点：这些「文件夹」是虚拟分组，不是真实目录★

材料的 `items` 行直接挂在「我的活动材料」项目下（`parent_id = NULL`），
前端**按 `activity_id` 分组**渲染成文件夹的样子。

为什么这样更干净（三个问题一次消失）：

| 如果做成真实目录 | 虚拟分组 |
|---|---|
| 活动改名 → 要同步改文件夹名（漏了就对不上） | 显示名直接读活动，**永远一致，零同步成本** |
| 要挡住「用户自己建文件夹 / 移动文件」 | **根本没有目录可建**，约束天然成立 |
| 同一天两个同名活动 → 文件夹重名要加序号 | 按 `activity_id` 分组，重名无所谓 |

排序按日期**倒序**（最近的在上）——这个区是用来回看的。

### J1. 它★不是一个项目★，只是技术上复用了项目表

| | 普通项目 | 我的活动材料 |
|---|---|---|
| 拉成员 / 授权 | ✅ | ❌ 只有我 |
| 建文件夹 / 在线编辑 / 版本历史 | ✅ | ❌ **什么都不能做** |
| 归档 / 删除项目 | ✅ | ❌ 系统建的，不能删 |
| 出现在项目列表里 | ✅ | ❌ 只在个人面板与活动详情里作为材料落点 |
| **复制到其他项目** | — | ✅ 这是它唯一的出口 |
| 平台超管直接读 | ✅（超管短路） | ❌ ★读不到，见 J1c★ |

**定位**：它是「个人活动产生的东西的存放处」，不是第二个工作区。
要拿去协作，就复制到真正的项目里。

⚠ ★上面这张表是「默认拒绝」的清单，不是「逐条挡掉」的清单★（2026-08-08 补）：
「我的活动材料」是复用项目表实现的，而以项目为作用域的入口有
**12 个 `/projects/{id}*` + 16 个 `/items/{id}*`**（后者经 `project_of` 反查项目，
在项目层做排除**完全拦不住**）。逐条打勾的清单在 553 处改名的同期一定会漏，
所以判据必须是**白名单**：`kind='materials'` 的项目只放行
「读树 / 读 item / 下载 / 复制出去 / 回收站还原」，其余一律拒。

### J1c. 超管★读不到★别人的材料区，救火走「影子账户」 ★2026-08-08 liaoruili 拍板★

**问题**：J1 承诺「只有我」，但 `perm.rs` 的超管短路让平台管理员对任何项目都是 Admin。
而材料区里会有体检报告、私人录音这类东西——「只有我」这句话在实现上不成立。

**决策：两件事一起做。**

1. ★堵掉直读★：超管短路对 `kind='materials'` 且不是自己的项目**不生效**，返回 404（不是 403，不泄露存在性）。
   超管仍看得到「这个人占用了 X GB」（配额页需要），但看不到文件名与内容。

2. ★补一条**有痕迹的**救火通道：影子账户（impersonation）★
   超管可以显式「以某个用户的身份查看」，进入后：
   - **全程只读**——所有写操作一律拒绝；
   - 看到的**就是那个人看到的**（影子模式下超管位恒为 false，不叠加任何特权）；
   - 进入与退出都写 `audit_log`；
   - 页面顶部**常驻横幅**「你正在以 X 的身份查看 · 退出」，不可关闭。

**为什么这样比「超管能直读」好**：救火通道从一个**隐形的超级权限**变成
**一条显式的、只读的、以本人视角的、留痕的路**。想看就必然留下记录，
而不是「想留痕就切身份、不想留痕就直接看」。

⚠ **只读必须做成一道闸，不是一张清单**：`/api` 整层已有一个鉴权中间件，
影子模式下在那里**按 HTTP 方法**拒绝一切非 GET 即可（已核实：34 个 GET handler 无一写库）。
这样以后**任何**新增的写接口都自动被挡住——清单会漏，闸不会。
★退出入口放在 `/api` 之外★（与 `/auth/logout` 同层），否则退出本身是非 GET、会被自己的闸挡住，
就得给闸开例外——而例外正是漏洞的来源。

⚠ **这条不进 M0**。M0 是纯改名，验收判据是「行为不变」，掺新功能进去会毁掉那个门禁。
它单独走一轨（动权限 → 至少轻量轨：技术设计 + 契约 + 测试）。

⚠ **验收有一个真实的阻塞**：E2E 通道目前只有 `e2e` 一个身份且非超管，
已实测确认网关会剥掉客户端伪造的 `X-Forwarded-Preferred-Username`（这是对的，说明平台的
strip-identity 中间件在干活）。所以影子账户**做不了 E2E 验收**，要等平台的多身份头。
★这条要写进技术设计的依赖里，不能标绿假装验过。★

### J1b. 删除：★删掉活动时连带删材料★

「我的活动材料」里**没有单独的删除入口**——材料始终依附于活动，
要删就删那条活动，它的材料跟着走（进回收站，30 天）。

概念上最干净：★这个区里的每一份材料都有主人（某条活动）★，不存在游离的文件。

⚠ **三条必须写清、否则实现会一刀切的边界**：

1. ★这条规则**只适用于「我的活动材料」**★。普通项目里删掉活动，
   **材料留在项目里**——那是项目的资产，不该被一次活动的删除带走
   （原 D10「会议材料进项目树的只读会议区」的精神）；
2. **已复制到别的项目的副本不受影响**：副本是独立的 items 行，
   引用计数保证底层 blob 不被误删。★这一点要在界面上说出来★，
   否则用户不敢删任何东西——「删了会不会把我复制到课题组那份也删了」；
3. UI 上要**说清没有单独删除入口**，否则用户找不到删除按钮会以为是 bug。

#### J1b-2. 材料区★自带回收站，删了能还原★ ★2026-08-08 liaoruili 拍板★

上面说「进回收站，30 天」，但**这个承诺原本兑现不了**：回收站入口在项目页、要 editor 权限，
而「我的活动材料」按 J1 根本不出现在项目列表里 —— 用户找不到那个页面。
只能删不能还原的回收站不是回收站。

- 「我的活动材料」页加一个**回收站 tab**，列出 30 天内删掉的活动；
- 还原 = **活动和它那批材料一起回来**；
- ★还原只还原「与它同一批被删的」★（按 `deleted_at` 当批次号）——
  这是 v0.4 回收站已经踩过并定下的做法：否则先前单独删掉的子项会跟着复活。

⚠ 顺带堵一个孤儿态：材料侧**不提供单独还原**。
否则会造出「item 活着、但它依附的活动还在回收站」的行——
而 J0b 的虚拟分组是按「日期 + 活动名」渲染的，这种行**根本显示不出来**：
用户看不见、删不掉，却一直占着配额。

### J2. 跨项目复制：★实现上几乎免费★

v0.4 已有内容寻址去重（`blobs/<sha256>` + 引用计数），
复制 = 在目标项目建一行 items 指向**同一个 blob**，存储一个字节都不增加（「秒传」用的就是这套）。

三条要处理的：
- 需要**目标项目的 editor 权限**（复制是往目标项目里写）；
- 复制后是**独立副本**：改名 / 删除互不影响（引用计数保证 blob 不被误删）；
- ★配额**同一 owner 内按 blob 去重，只算一份**★（2026-08-08 liaoruili 拍板，**推翻本条原文**）。
  原文写的是「各算一份」，理由是「否则谁先传谁倒霉，而且用户无法理解自己项目的配额为什么被**别人的**文件占了」——
  ★那条理由是 `created_by` 归属时代的论证★，在 L3 拍板的 **owner 模型**下已经不成立：
  两个副本本来就都记在我自己头上，不存在「被别人占」这回事。
  而按物理事实，内容寻址下盘上**本来就只有一份**；为我自己的同一份文件收两次费，用户解释不通。
  - 同一 owner 内复制 / 秒传 → **用量不变**；
  - 跨 owner 仍然**各算各的**（我复制到别人的项目，算那个项目 owner 的，与我无关）。
  - 实现：配额 SQL 按 `GROUP BY s3_key`（**不带 project_id**）。

### J3. 「项目」这个词★不改★

考虑过改回「空间」或改成「工作区」，结论是不改：

★**归档 = 做完了**（B2）——会归档、有始有终，这正是「项目」的含义★。
「空间」不会「做完」，给它一个归档按钮是自相矛盾的。

「项目装不下的东西」在新模型里各有出路，只是落点不在这里：

| 我想表达 | 落在哪 |
|---|---|
| 这件事**性质**是什么（读文献 / 健身 / 开会） | **活动类型**（A 节） |
| 这件事属于哪个**协作单元**（课题组 / 某篇论文） | **项目** |
| 纯粹是我自己的事 | 不关联项目，材料落「我的活动材料」 |

→ 「健身」不是一个项目，它是一个**活动类型**；
按类型统计「我这季度健身 20 小时」正是 A0 想要的。

### J4. 日程图例改成★公开 / 不公开★，去掉「项目」二字

判据从**项目的可见性**换成**活动自己的可见性**。

**为什么这是对的**：活动现在可以不关联任何项目（A4），
「只关联了私密项目」这个判据对它根本不适用——它连项目都没有。

★副产品：`is_private` 这个字段整个消失★——不必再从关联项目反推。
（那正是 v0.4 踩过「字段声明了但 SQL 没算，`#[sqlx(default)]` 静静给 false」的地方。
判据少一层推导，就少一处能悄悄错掉的地方。）

⚠ **注意「不公开」内部其实有两档**，但日历上不必区分（对我而言都是「别人看不到」）：
- 活动 `visibility=private` 且有参与人 → **参与人可见**；
- 不关联项目的个人活动 → **仅自己可见**。

---

## L. 类型的生命周期与配额 ★2026-08-07 已确认★

### L0. ★发不发通知，只看时间过没过★

**问题**（故事地图 §5 问题 1 查出来的）：昨天张三来找我聊了两小时，
今天我想记下来**并记上张三**——统计时我想知道和谁花了时间。

**决策**（liaoruili 2026-08-07 原话：「就参与人；复用之前的呀，只是不发通知而已，
已经过了的就不发通知，这样不就全部统一了吗」）：

> ★**参与人就是参与人，一套机制**★。
> **唯一的判据是活动开始时间过没过**：
> - 时间在**未来** → 照常发邀请、进对方日历、要应答；
> - 时间在**过去** → **不发任何通知**，纯粹是我这边的一笔记录。

⚠ 我原本的设计（`status='noted'` 新状态 + 「一起的人」这个新词）**已废弃**——
那是把简单的事复杂化了：造了一个新状态、一个新概念、还要在两处维护它们的差别，
而这一切用「时间过没过」一个判据就够了。

**推论**（都很自然，不需要额外规则）：
- 补录一场临时会 = 用「会议」类型、填过去的时间、填上参与人 → 不发通知；
- 补录一条个人记录带参与人 → 同样不发通知；
- ★对方的统计里不计入★：他从没答应过，也可能不知道你记了这一笔
  （统计只算「我参与且没拒绝」的，补录的活动对他而言连 pending 都没有）。

### L0b. 补录的活动，对方★完全看不见★ ★2026-08-08 liaoruili 拍板★

L0 只说了「不发通知」。**「不发通知」不等于「看不见」**——这半边不写死，
实现会照默认走：参与人行以 `status='pending'` 落库，于是

- 这条活动**直接出现在对方日历上**（可见性判据含「我是参会人」），`pending` 在前端就是待应答红色；
- **计入对方统计**（口径是 `status <> 'declined'`），与上面 L0 那句「对方的统计里不计入」正面冲突；
- 他还会在待办卡里看到一条「等你答复」。

★这不只是不一致，是一条真实的滥用路径★：我可以建一条「昨天 9:00–18:00 和张三讨论」的补录活动、
把张三填进参与人 —— 他**不会收到任何通知**，但日历上凭空多了一条 9 小时的待应答条目、
季度统计凭空多 9 小时，而且他不知道是谁加的。规模化就是对全所人员日历的静默污染。

**决策：补录的活动只存在于我这边。** 对方不进日历、不进统计、不进待办、不产生忙闲、不要应答。

**判据**★2026-08-08 liaoruili 拍板改用 `notified_at`，推翻下面原来那条★（不新造状态——`status='noted'` 仍然废弃）：

> ★参与人行上记一列 `notified_at`：**非空 = 这个人被通知过 = 这条活动对他可见**★。
> 写入时机（判据抽成纯函数 `should_notify(starts_at)`，两个写入点共用）：
> 活动在未来（含 5 分钟容差）→ 发站内信 + 置位；否则（补录）→ 不发 + 留 NULL。
> ⚠ **写入点有两个**：`POST /api/activities`（create 自己插三批参与人行）与
> `PUT /api/activities/{id}/participants`。**读出点有四个**：日历 / 统计 / 待办 / 忙闲。

**为什么推翻原来那条**（原文：「建的时候 `starts_at` 就已经过去 = 补录，即 `created_at > starts_at`，
都由 `created_at` 与 `starts_at` 现算，**不存第二份**」）：

「不存第二份」这个直觉是对的，但★这个判据本身会随改期翻转★——不管是现算还是存生成列：

- 我建了下周三的组会，8 个人接受了；
- 我把时间改到上周（**合法的纠错**，改期本来就是允许的）；
- → `created_at > starts_at` 由假变真 → ★这场会从那 8 个人的日历上无声消失★，无通知、无审计、无报错。

还有一个更常见的边界：`created_at` 取的是事务开始时刻，`starts_at` 是客户端算的。
用户点「现在开会」→ 网络往返几十到几百毫秒 → `created_at > starts_at`
→ **刚建的会对所有被邀请的人隐形**。（现行代码给「不能排过去的会」留了 5 分钟容差，
注释写着「填表本身要花时间…卡死到秒会让人白填一轮」；而 F0 把那条校验删了。）

★`notified_at` 记的是**写入那一刻的事实**★：之后改时间、改标题都不会动它，
而且它天然是 **per-person** 的——同一条活动，先邀的人看得见、后来补加的人看不见，
这正是 L0「参与人就是参与人，一套机制」想要的效果。

⚠ **残留边界，实现时要处理**（两条都得有对策，不能装看不见）：
1. 「补录 → 事后改到未来」：`notified_at` 不会自己回填，于是活动到了未来仍对所有人不可见、不占忙闲；
   而改期会给他们发「时间已改，请重新答复」——★收到信却看不到会★。
   → 改期时若活动变成未来且该参与人 `notified_at IS NULL`，**补发一次邀请并置位**。
2. 「未来 → 改到过去」：`notified_at` 不翻转（这正是它相对原判据的进步）。
   但由此 L0b 想堵的统计污染变成**两步可达**：建未来的会拉上张三 → 他被通知一次 →
   改成「昨天 9:00–18:00」→ 改期把他重置为 `pending`，而统计口径是 `status <> 'declined'`
   → ★他季度统计凭空多 9 小时★。
   → 统计口径收紧成**只认 `accepted`**（他明确答应过才算他的时间）。

- 与 `is_backfilled` 同源（那个是 7 天阈值，这个是 0 阈值），**都由 `created_at` 与 `starts_at` 现算，不存第二份**；
- ★开完的正常会议不受影响★：它建的时候 `starts_at` 在未来，对方当初收到过邀请、答复过，
  会开完之后照常留在他日历上、照常计入他统计。这正是「判据用**建的时候**而不是**现在**」的原因。
- 实现上**收口在一处**（一个 `fn 对他可见(activity, viewer) -> bool`），
  作用于日历列表 / 统计 / 待办卡 / 忙闲四条读路径。★不要在四个地方各写一遍 SQL★。

### L1. 活动类型★软删除★，历史照常显示

用户建了「读文献」、用了 20 次，然后想删掉它。

**决策**：类型行标 `deleted_at`——
- **历史活动照常显示它的类型名**（★统计不断档★）；
- 新建活动时不再出现在下拉里。

不做「删除时要求把历史转到别的类型」：那是把系统的问题丢给用户。

### L2. ★不提供「改类型」功能★——要改就删了重建

**为什么不做**：类型决定能力（A1），改类型意味着能力位变了——
会议改成个人日程，参与人往哪去？纪要留不留？
任何答案都是**静默丢数据**或**维护一张兼容矩阵**。

liaoruili 原话：「改类型相当于删除重建」。那就**如实呈现**：
不给这个按钮，用户想改就自己删掉重建。
★诚实的「不能做」好过一个会悄悄吃掉数据的「能做」★。

### L3. 配额★算到项目主持人（owner）头上★

**推翻两版**：v0.4 的「每项目 10GiB」，以及我 2026-08-07 初稿写的「按上传者 `created_by` 算」。

liaoruili 的三个反问把后者否掉了，每一个都戳在要害上：

> 「我为这个项目做贡献，我把自己的资料分享出来，还要算我的额度？」
> 「项目被删除了怎么办？我被剔除项目了怎么办？我都找不到之前我上传的了呀。」

★按上传者算 = 惩罚贡献者★；而且它会造出一个**看不见也删不掉的用量**——
被移出项目之后，那些字节仍算我的，我既列不出来（要 viewer）也删不掉（要 editor）。

**最终口径**：

| | 规则 |
|---|---|
| 我的用量 | 我**作为 owner** 的所有项目里的全部内容（★不管是谁传的★）+ 我的「我的活动材料」 |
| 我加入别人的项目往里传 | 算**那个项目 owner** 的，与我无关 |
| 我被移出项目 | 用量**不变**（本来就没算我的） |
| 项目被删 | owner 的用量释放 |
| 默认额度 | 10 GiB / 人，**超管可对单个人升降级** |

★它同时堵住了旧「项目配额」的洞★：旧模型是每个项目 10GiB，一个人建 50 个项目就拿到 500G；
新模型是**一个人名下所有项目加起来 10GiB**，建再多项目也不会多出一个字节的额度。

**实现上反而更简单**：按 `project_id` 聚合（现有索引就有）再按 `projects.owner` 汇总，
不需要 `items(created_by)` 索引，也不需要给 `item_versions` 加「这一版的作者」列
（那是按上传者算才会有的问题）。

⚠ **回收站仍然计入**（沿用 v0.4）：占着盘就该算。
所以配额汇总是全系统唯一**刻意不带 `deleted_at IS NULL`** 的读路径——
这条例外必须写在 SQL 旁边，否则下一个人会照着「反漂移纪律」把它"修"掉。

### L3b. 转移主持人时：★允许超额，超了只能删不能传★

项目转移会把它的占用从原主持人挪到接手人。接手人额度不够时**不拒绝转移**：

- 转移照常完成，接手人进入「超额」状态：**能删、不能新传**，直到降下来或找管理员扩额；
- ★交接本身不该被配额卡住★——毕业、离职那一天人已经走了，转不了就成了无主项目。

这条也顺带定义了「超额」这个状态本身：它不只在转移时出现（超管调低额度同样会造成），
所以判据统一成「**已用 > 额度时，一切新增写入拒绝，删除照常**」。

---

## K. 统计：★按类型★是主视角 ★2026-08-07 已确认★

活动泛化之后，「我这周开会 6h、读文献 12h、写作 8h」比
「AI 项目 8h、数据治理 6h」更接近人想知道的事 ——
**按项目分组回答「我为哪个团队花了时间」，按类型分组回答「我在做什么」**，
后者才是个人视角的主问题。

个人面板放**两张表并列**：

| 表 | 回答 |
|---|---|
| **按类型**（新，主） | 我的时间都花在什么性质的事上 |
| 按项目（现有） | 我为哪些协作单元花了时间 |

口径不变：**D5 三级回退**（录制 > 手工补录 > 排程），并显示口径来源。

⚠ 两张表的**合计对不上是正常的**：按类型不会重复计（一个活动只有一个类型），
按项目会重复计（一个活动可关联多个项目，D6「分组展开」）——
★所以「按项目」那张表的合计不等于总数，这件事必须在界面上说出来★（v0.4 已这么做，保留）。

---

## H. 继承自 v0.4 的能力清单

> ★这一节是重构里最容易出事的地方★：**没写进新文档、但已经在用的功能**最容易被丢掉。
> 下面把 v0.4 已实现的东西逐条过一遍，标明 v0.5 怎么处置。

### H1. 原样保留（只做改名）

| 能力 | 说明 |
|---|---|
| 项目 / 成员 / 五档角色 / 权限诊断 | 不动 |
| 内容树、文档编辑与版本历史、回收站（软删除 30 天） | 不动 |
| 大文件直传、断点续传、秒传去重、流式下载 | 不动 |
| 公开分享（提取码 / 有效期 / 次数上限 / 访问统计） | 不动；★移出成员连带撤销其公开链接★仍是硬要求 |
| 忙闲查询、冲突提示、四态应答（接受/拒绝/待定/建议改期）、采纳与驳回 | 改名后保留 |
| 讨论区（公开 + 私聊）、私聊未读与「全部标记已读」 | 改名后保留 |
| 公开活动广场 + 自助旁听（原「公开会议」） | 保留；★只列我还没有关系的活动★（v0.4.30 修正） |
| 站内信通知（建/邀/改期/改链接/取消/改期建议/纪要生成） | 保留 |
| 转移主持人**需对方接受**、待办卡（邀请 / 私聊未读 / 转移） | 保留 |
| 自动转写 + AI 摘要（挂在**文件**上：`transcripts` / `summaries`） | 保留，★**所有类型**无条件自动★（2026-08-08 拍板，见 H2） |
| 正式纪要（挂在**活动**上：`meeting_minutes`，记录员按模板整理） | 保留，★仅 `has_minutes` 的类型★（A1 就是这么定义这个能力位的） |
| 项目归档、个人面板、我的分享、超管面、审计 | 保留 |

### H2. 保留但要按新模型改造

| 能力 | 怎么改 |
|---|---|
| 记录员必填 | 改为**仅 `has_minutes` 的类型**必填 |
| 必须关联 ≥1 个项目 | 改为**仅 `needs_project` 的类型**要求 |
| 参与人 / 应答 / 旁听 | **所有类型都有**；★过去的活动不发通知、不要应答★（L0） |
| 材料 / 录制 / 转写 | **所有类型都有**；不关联项目的活动，材料落「我的活动材料」（J0） |
| **自动转写的触发范围** | ★所有类型，无条件自动★（2026-08-08 liaoruili 拍板）。⚠ 这一条原本与 H1 那行「仅 `has_minutes`」**自相矛盾**（一条说只有会议转、一条说所有类型转），已统一到「所有类型」，并把**转写/摘要**（挂文件）与**正式纪要**（挂活动，仅 `has_minutes`）在 H1 里拆成两行——它们在库里本来就是两套表。⚠ 配套：上传音视频时**必须提示一句「将自动转写为文字」**——个人活动的音频（看病录音、访谈）会因此被送去平台 ASR + LLM 网关并落 `transcripts`/`summaries`，这是全系统唯一一条把用户内容送出 congrove 边界的路径，用户至少要知情 |
| 「个人日程」用私密项目实现 | 改为**不关联项目 + 仅自己可见**（A4），不再需要为记日程建项目 |
| 日历配色（公开青 / 私密紫 / 待应答红） | 叠加 C 节的身份图标与 B 节的归档淡化 |

### H3. v0.4.35~37（PR #9，★已合并进 dev★ 66dc8be）

D5 时长三级回退与口径来源 / 会议粒度材料权限（禁下载·禁分享）/ 必参·选参 /
项目统计与 D6 去重 / 「记录员不能改公开性」权限修正。

**已合并**。重构时★照着搬那些决策注释★（为什么取 max 不取 sum、为什么叠加不覆盖、
为什么参会率分母不含旁听者）—— 丢掉它们等于把同样的坑再踩一遍。

### H4. 明确废弃

| 废弃 | 原因 |
|---|---|
| 「临时参会人（guest）」 | 2026-08-07 已作废（推翻 D8），旁听者就是临时人 |
| D17 的「归档项目的会不进日历」 | 被 B0 推翻 |
| 迁移文件 0001~0007 | ★库已清空，压成一个干净的 `0001_init.sql`★ |

---

## I. 数据模型草案（全新建，不做迁移）

```sql
-- ★活动类型：能力位在这里，是全系统「一个活动能做什么」的唯一真相源★
activity_types(
  id, owner              -- owner=NULL 表示系统预置（会议 / 个人日程）
  deleted_at,            -- ★软删除★:历史活动照常显示类型名,新建时不再出现（L1）
  name,                  -- 「会议」「读文献」…
  -- ★只有三个能力位★（A1，2026-08-07 精简）：参与人/材料/补录对所有类型统一开放
  has_minutes,           -- 纪要与记录员（决定 recorder 是否必填）
  needs_project,         -- 必须关联项目（决定材料有没有权限归属，D3）
  busy_default,          -- 默认占不占忙闲（用户自建时唯一可选的开关 A3）
  created_at
)
-- 唯一约束: (COALESCE(owner,''), name) WHERE deleted_at IS NULL；预置行 owner IS NULL
-- ⚠ 原草案里的 `sort` 列已去掉：按 id 排够用，多一列就多一处要维护的顺序真相源。

activities(
  id, type_id, title, agenda,   -- ★沿用 v0.4 的列名 agenda★（原草案写 note）：
                                --   会议界面上叫「议程」、个人活动界面上叫「备注」，同一列两个标签。
                                --   改名一次要多动 553 处里的一部分，而它没换语义，不值得。
  organizer, recorder,       -- recorder 仅 has_minutes 时非空
  starts_at, ends_at,        -- timestamptz（UTC）
  timezone,                  -- ★IANA 名★，输入解释 + 周期展开基准（E1）
  busy,                      -- 本场是否占忙闲（默认取类型的 busy_default）
  visibility,                -- private / public
  status,                    -- active / canceled（★取消 ≠ 删除，见下面 deleted_at★）
  remind_minutes,            -- NULL=用个人默认；0=不提醒（F3）
  remind_done_at,            -- ★提醒**已处理**的时刻★（2026-08-08 订正，原草案写 reminded_at
                             --   「已发提醒的时刻」）：语义必须是「处理过」而不是「发过」——
                             --   否则 remind_minutes=0（明确不提醒）的活动永远不会被置位，
                             --   会永远躺在扫描索引里被反复捞出、反复判断、反复跳过，索引不收敛。
                             --   ⚠ 改了 starts_at 或 remind_minutes 要置回 NULL，否则再也不提醒。
  actual_minutes, actual_by, -- D5 第 2 级：会后补录的实际时长
  no_download, no_share,     -- 活动粒度的材料策略（与项目级叠加）
  rrule,                     -- 周期规则（本版可留空）
  deleted_at, deleted_by,    -- ★软删除（2026-08-08 补）★：原草案漏了这两列。
                             --   J1b 要求「删掉活动 → 材料跟着进回收站 30 天」= 活动必须能真删，
                             --   而全系统纪律是「所有删除都是软删除」。
                             --   ★与 status='canceled' 正交，两者都要有★：
                             --   取消 = 留在日历上标灰（留档）；删除 = 整条消失、材料进回收站。
  created_at, updated_at,
  is_backfilled              -- ★生成列★：created_at - starts_at > 7 天（F1 的「过久给个确认」）。
                             --   用生成列而不是布尔列，是为了不造第二个真相源——改了时间它自己跟着变。
)
-- CHECK: ends_at > starts_at
-- CHECK: ends_at <= starts_at + 30 天  ★上界（2026-08-08 liaoruili 拍板，见 F4）★

activity_projects(activity_id, project_id)      -- 多对多；★可以是 0 行★（A4：活动可以不关联项目）
-- ★status 是**五态**★（pending/accepted/declined/**tentative**/counter）
--   —— 2026-08-08 订正：原草案写「仍是四态」漏了 tentative（「待定」），
--   而它在 v0.4 里是实现了的（web/src/todo-card.tsx 的「待定」按钮）。
--   照四态写 CHECK，重构后「待定」一点就 500，且报错指向数据库不指向按钮。
-- 不为补录另造状态 —— 发不发通知只看 activities.starts_at 过没过（L0）
activity_participants(activity_id, username, kind, required, status, counter_*, responded_at)
activity_minutes(activity_id, ...)              -- 同 v0.4
activity_messages / activity_reads / activity_link_history   -- 同 v0.4，改名

user_prefs(username, timezone, default_remind_minutes)
user_quota(username, quota_bytes, updated_by, updated_at)
  -- ★拆两张表★：配额是超管授予的，不是用户偏好。分表 = 「配额不可自改」成为结构事实而不是纪律
  -- （否则 PUT /api/me/prefs 一个全行 upsert 就把自己的配额改回默认了）
-- ⚠ projects 表★去掉 quota_bytes★：配额只有一个真相源
-- 用量口径（L3）：SELECT sum(...) FROM items JOIN projects p ON p.id=items.project_id
--                  WHERE p.owner = $1   ★算 owner 的，不是 created_by★

items(... , activity_id, is_recording, ...)     -- meeting_id → activity_id
```

⚠ **建表时就要带上的两条**（别等以后补）：
- `activities.starts_at/ends_at` 的索引要带 `WHERE status='active'`（忙闲与日历是最热路径）；
- 所有读内容的查询都要 `deleted_at IS NULL`——
  ★这条纪律在 v0.3.55 一次补了 11 处、v0.4.28 又补了 5 处★，新建表时就写进去，别再补第三次。

---

## 开放问题（★相位 2 的硬闸：全部关闭才能进相位 3★）

> **2026-08-07：6 条全部关闭。** 下表保留作过程记录，正式结论都已写进对应章节。

| # | 问题 | 我的默认（不反对即采纳） |
|---|---|---|
| ~~1~~ | ~~跨类型的时长统计怎么呈现~~ | ★已关闭★：按类型 + 按项目**两张表并列**，按类型为主（见 K 节） |
| ~~2~~ | ~~PR #9 的功能在 v0.5 里保留哪些~~ | ★已关闭★：**全保留**，PR #9 已于 2026-08-07 合并进 dev（66dc8be），v0.5 在其上重构 |
| ~~3~~ | ~~B2 拒绝归档时「未开始的活动」含不含已取消的~~ | ★已关闭★：**不含**——取消了就不算有安排（已写进 B2） |
| ~~4~~ | ~~不关联项目的个人活动，别人能否在忙闲里看到~~ | ★已关闭★：能，但只是无标题时间块（A4） |
| ~~5~~ | ~~个人活动要不要也能上传材料~~ | ★已关闭★：**能**，落「我的活动材料」（见 J 节） |
| ~~6~~ | ~~「我的活动材料」里传错了的文件能不能删~~ | ★已关闭★：删活动时连带删（J1b） |

---

## 相位状态

- [x] **相位 0 需求 PRD** —— 本文（背景/目标/**非目标**/角色/概念模型/A–L 决策）
- [x] **相位 1 故事地图** —— `STORY-MAP-v0.5.md`（骨干八站 → M0~M4 五期，
      ★沿旅程查出 6 个 PRD 漏项，已全部回填成 L 节★）
- [x] **相位 2 需求确认** —— ★6 条开放问题全部关闭★（2026-08-07 逐条与 liaoruili 对齐）；
      ★2026-08-08 又开又关 5 条★——它们是**相位 4 的技术设计评审倒逼出来的**
      （见下面「回填的教训」），已逐条请 liaoruili 拍板，见 F4 / J1b-2 / J1c / L0b / H2。
- [x] **相位 3 UI 原型** —— `prototype-v0.5.html`（六视图，★只画新东西★；
      截图在 `unit_tests/congrove/screenshots/_prototype-v0.5/`）
- [ ] **相位 4 技术设计** —— `TECH-DESIGN-v0.5-M0.md`。
      ★v1 与 v2 均被四路独立同行评审判为**不通过**★（2026-08-08），正在返工到 v3。
- [ ] 相位 5 实现
- [ ] 相位 6 验收

### ★回填的教训：相位 0 的产物错着，下游会原样踩回去★（2026-08-08）

本次回填里最值得记的不是那 5 条新决策，而是**本文自己有三处事实错误**，
而它们是相位 4 的评审替我抓出来的——不是我复查抓出来的：

| 本文写的 | 事实 | 照它写代码会怎样 |
|---|---|---|
| §I「★status 仍是四态★」 | 五态，漏了 `tentative`（「待定」，v0.4 早已实现） | 建表 CHECK 照抄 → **「待定」按钮一点就 500**，报错还指向数据库不指向按钮 |
| §I `reminded_at`「已发提醒的时刻」 | 语义必须是「已**处理**」 | `remind_minutes=0` 的活动永不置位 → 永远躺在扫描索引里被反复捞出 |
| §I `activities` 无 `deleted_at` | J1b 要求活动能真删 | 「删活动连带材料进回收站」在数据层无处落脚 |

★所以纪律是：技术设计里发现的 PRD 错误，必须回填到 PRD，不能只在技术设计里注一句。★
相位 0 是签核产物，下一个照它生成代码的人（或 AI）读的是它，不是相位 4 的勘误。
