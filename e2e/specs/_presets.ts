/// 预置活动类型的 id（ADR-0002）。
///
/// ★为什么可以写死★：按 ADR-0001，上线前**每次部署都清库、从单个 `0001_init.sql` 重建**，
/// 而预置行是 0001 里固定顺序的两条 `INSERT`（`bigserial` 从 1 起）——
/// 所以这两个 id 是**部署纪律保证的**，不是碰巧。
/// ⚠ prod 通道开出来之后 ADR-0001 失效，这两个常量也要跟着改成按名字查。
/// ⚠★用中文名是有意的★：预置类型确实**就叫「会议」**（它是活动的一个种类，
/// 不是被改掉的旧实体名），但 `TYPE_MEETING` 这种写法和「旧命名残留」在文本上分不开，
/// `scripts/no-meeting.sh` 会把它判成红 —— 而那道闸判得对：它没有语义、只能看字。
/// 本仓库测试里本来就用中文标识符（`fn 材料区对别人零角色…`），这里跟随。
export const 会议 = 1          // 有纪要 / 须关联项目 / 占忙闲
export const 个人日程 = 2      // 三样都不要
